$(function() {
  $('.slick').slick({
      dots: true,
      autoplay: true,
      autoplaySpeed: 1000,
  });
  $('.slick-dots li').on('mouseover', function() {
    $('.slick').slick('goTo', $(this).index());
  });
});